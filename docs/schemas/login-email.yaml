type: object
properties:
  email:
    type: string
    format: email
    description: User email
    example: user@email.com
  password:
    type: string
    description: User password
required:
  - email
  - password
